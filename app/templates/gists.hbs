<AstMaker 
  @mode={{codemod.mode}} 
  @code={{codemod.sourceCode}}
  @dest={{codemod.destCode}}
  @nodeOp={{codemod.opCode}}
  @parser={{codemod.parser}}
  @gistTransform={{codemod.transform}}>
</AstMaker>
{{version}}